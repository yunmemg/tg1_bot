# 音乐机器人安装教程

从零开始，一步步完成 Telegram 音乐机器人的部署，最终托管在 Railway 上，7×24 小时在线运行。

## 目录

1. [项目简介](#1-项目简介)
2. [准备工作](#2-准备工作)
3. [创建 Telegram 机器人](#3-创建-telegram-机器人)
4. [本地运行（可选）](#4-本地运行可选)
5. [推送代码到 GitHub](#5-推送代码到-github)
6. [Railway 部署](#6-railway-部署)
7. [管理员扫码登录](#7-管理员扫码登录)
8. [验证机器人运行](#8-验证机器人运行)
9. [常见问题排查](#9-常见问题排查)
10. [完整配置清单](#10-完整配置清单)

---

## 1. 项目简介

本项目是一个 Telegram 音乐机器人，支持 **网易云音乐 / QQ音乐 / 汽水音乐** 三平台搜索、点歌与收藏。

### 核心能力

| 能力 | 说明 |
|------|------|
| 跨平台搜索 | `/search` 并行搜索网易云、QQ、汽水三平台 |
| 点歌播放 | `/dian` 直接播放最匹配歌曲，含封面、歌名、歌手 |
| 个人收藏 | `/fav` 收藏歌单，可播放/删除 |
| VIP 完整播放 | 管理员扫码登录网易云/QQ后，**所有用户**点歌可完整播放原时长音频 |

### 登录机制

- 只需 **管理员** 一人扫码登录网易云/QQ 账号，登录凭证全局共享
- 所有用户点歌时自动使用管理员登录态解析 VIP/会员歌曲
- 汽水音乐不接登录，保留免费解析链路

---

## 2. 准备工作

需要提前注册好的三个账号：

| 平台 | 用途 | 注册地址 |
|------|------|---------|
| Telegram | 创建机器人、日常使用 | 手机应用商店下载 |
| GitHub | 存放代码 | https://github.com |
| Railway | 托管机器人 | https://railway.app |

---

## 3. 创建 Telegram 机器人

1. 在 Telegram 中搜索 **@BotFather**（官方机器人，带蓝色认证标识）

2. 向 BotFather 发送 `/newbot`

3. 按提示依次填写：
   - **机器人昵称**：例如 `我的音乐机器人`
   - **机器人用户名**：必须以 `bot` 结尾，例如 `my_music_bot_2026`

4. 创建成功后，BotFather 会返回一段文字，其中包含 **Token**：

   ```
   Use this token to access the HTTP API:
   123456789:AAF-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
   ```

   ⚠️ **Token 是你的机器人唯一凭证，相当于密码，不要泄露给他人。**

5. 记下 Token，后面要用。此时去 Telegram 搜索你刚创建的机器人用户名，点「开始」，就能看到它了。

---

## 4. 本地运行（可选）

想在本地先跑起来看看效果，可以按以下步骤（也可以直接跳到第 6 节部署到 Railway）。

### 4.1 获取管理员用户 ID

机器人的「管理员」指 Telegram 用户。获取方式：

1. 在 Telegram 中向 [@userinfobot](https://t.me/userinfobot) 发送任意消息，它会返回你的用户 ID（一串数字）

### 4.2 配置环境变量

```bash
# 复制配置模板
cp .env.example .env

# 编辑 .env，填入你的信息
```

`.env` 内容示例：

```env
BOT_TOKEN=你的机器人Token

# 可选：只允许指定用户使用机器人
# ALLOWED_USER_IDS=111111111,222222222

# 可选：管理员用户 ID（仅他们可执行 /login 扫码登录）
# ADMIN_IDS=111111111
```

### 4.3 启动

```bash
bash start.sh
```

脚本会自动创建虚拟环境、安装依赖并启动机器人。看到 `音乐机器人启动成功，等待消息…` 即为成功。

---

## 5. 推送代码到 GitHub

### 5.1 准备仓库

1. 登录 GitHub，点击右上角 **+ → New repository**

2. 仓库名随意，例如 `music-bot`，选择 **Public**（或 Private 均可）

3. 创建完成后，进入仓库空页面，记录下仓库地址（形如 `https://github.com/你的用户名/music-bot.git`）

### 5.2 推送项目代码

在本地电脑的终端中执行（需安装 Git）：

```bash
# 进入项目目录（music-bot 目录，含 Dockerfile 和 bot/ 子目录）
cd music-bot

# 初始化 git 仓库
git init

# 关联远程仓库（替换为你的仓库地址）
git remote add origin https://github.com/你的用户名/music-bot.git

# 添加所有文件并提交
git add .
git commit -m "init: 音乐机器人"

# 推送到 GitHub
git push -u origin main
```

> 如果提示需要登录，按提示在弹出的浏览器窗口中完成 GitHub 授权即可。

---

## 6. Railway 部署

### 6.1 新建项目

1. 登录 [Railway](https://railway.app)，使用 GitHub 账号登录

2. 点击 **New Project**

3. 选择 **Deploy from GitHub repo**

4. 按提示授权 Railway 访问你的 GitHub，然后选择刚创建的 `music-bot` 仓库

5. Railway 会自动检测到项目根目录的 `Dockerfile` 并开始构建

### 6.2 配置环境变量

构建完成后（可能需要 2-5 分钟），进入服务页面：

1. 点击顶部服务名进入服务详情

2. 切到 **Variables**（变量）标签页

3. 点击 **New Variable**，添加以下变量：

   | 变量名 | 值 |
   |--------|-----|
   | `BOT_TOKEN` | 第 3 步从 BotFather 获取的 Token |

   可选变量：

   | 变量名 | 说明 |
   |--------|------|
   | `ADMIN_IDS` | 管理员用户 ID，逗号分隔（仅这些人可执行 `/login`；留空则所有人可登录） |
   | `ALLOWED_USER_IDS` | 用户白名单，逗号分隔（留空则所有人可用） |

   添加完成后，服务会自动重新部署一次。

### 6.3 挂载持久化存储（重要）

机器人会把「用户收藏歌单」和「管理员登录凭证」存在 SQLite 数据库中。Railway 的容器磁盘是临时的，重启会清空，所以必须挂载持久卷：

1. 切到 **Settings**（设置）标签页

2. 找到 **Volumes** 区域，点击 **Add Volume**

3. **Mount Path** 填写：`/app/data`

4. 点击创建

5. 回到 **Variables**，添加第二个变量：

   | 变量名 | 值 |
   |--------|-----|
   | `DB_PATH` | `/app/data/musicbot.db` |

   保存后等待自动重新部署。

### 6.4 确认启动

1. 切到 **Deployments**（部署）标签页

2. 找到最新一次部署，点击进入

3. 点击 **View Logs**（查看日志）

4. 看到如下日志即表示启动成功：

   ```
   [INFO] 音乐机器人启动成功，等待消息…
   ```

---

## 7. 管理员扫码登录

登录凭证全局共享，只需管理员操作一次，所有用户点歌自动使用：

1. 管理员在机器人对话中发送 `/login`

2. 选择「网易云音乐」或「QQ音乐」

3. 用手机 App 扫描机器人发送的二维码

4. 确认登录后，机器人提示登录成功，凭证自动保存

5. 用 `/login_status` 查看各平台登录状态，`/logout` 可退出登录

> 建议同时登录网易云和 QQ 两个平台，这样两个平台的 VIP/会员歌曲都能完整播放。

---

## 8. 验证机器人运行

1. 回到 Telegram，打开你创建的机器人对话

2. 发送 `/help`，机器人应回复使用说明

3. 发送 `/dian 晴天`，稍等几秒，机器人应发送歌曲音频

4. 发送 `/search 周杰伦`，查看跨平台搜索结果

5. 点歌后点击「❤️ 收藏」，再发送 `/fav` 确认歌单功能正常

6. 管理员发送 `/login` 扫码登录后，再点播一首付费歌曲，应能完整播放原时长

---

## 9. 常见问题排查

### Q1：部署后日志报 `401 Unauthorized`

`BOT_TOKEN` 填写错误或已失效。重新在 BotFather 执行 `/token` 获取新的 Token，更新环境变量。

### Q2：日志没有「启动成功」字样

说明构建未完成或启动报错。打开 **View Logs** 查看具体错误信息，通常在日志末尾能看到 Python 报错堆栈。

### Q3：点歌时提示「付费/VIP 歌曲无法获取」

未登录时付费歌曲无法获取完整播放链接，属正常现象。由管理员发送 `/login` 扫码登录网易云或 QQ 音乐后即可完整播放；也可在其它平台搜索该歌曲。

### Q4：重启后收藏的歌单/登录凭证没了

没有挂载 Volume 或 `DB_PATH` 未指向 `/app/data`。按 [6.3 节](#63-挂载持久化存储重要) 重新配置。

### Q5：机器人响应很慢

免费音乐解析 API 有频率限制。点歌不要太频繁，间隔几秒即可。

### Q6：修改代码后如何更新？

把改动 `git push` 到 GitHub，Railway 检测到仓库更新会自动重新构建部署。

### Q7：二维码扫码后提示超时？

二维码有效期为 3 分钟，请在有效期内完成扫码。若已超时，重新发送 `/login`。

---

## 10. 完整配置清单

| 项目 | 值 |
|------|-----|
| Bot Token | 从 @BotFather 获取 |
| 构建方式 | Dockerfile（自动检测） |
| 启动命令 | `python -m bot.main`（railway.json 已配置） |
| 必填变量 | `BOT_TOKEN` |
| 推荐变量 | `ADMIN_IDS`（登录权限）、`DB_PATH=/app/data/musicbot.db` |
| 可选变量 | `ALLOWED_USER_IDS`（用户白名单） |
| 持久卷挂载点 | `/app/data` |
| 登录方式 | 管理员 `/login` 扫码，凭证全局共享 |
